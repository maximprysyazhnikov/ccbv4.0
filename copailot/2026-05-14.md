# 2026-05-14 - crypto scalping recovery notes

## Problem

Crypto scalping stopped opening trades while metals continued to trade. The live DB showed that crypto was mostly blocked by `DIRECTION_EDGE_GUARD`, then after reset by gate and hard-blocker filters.

## Diagnosis

- Live DB: `storage/bot_live_v2.db`.
- Dominant blocker before fix: `DIRECTION_EDGE_GUARD`.
- The existing performance reset (`autopost_perf_epoch_ts`) affected low-WR pause/recovery, but direction-edge still used old closed trades.
- After the code fix and reset, direction-edge moved to warmup and no longer blocked old crypto losses.
- Current blockers are normal quality filters: low volume, stretched VWAP, overbought RSI, late BB%B, weak Stoch.

## Changes Applied

- Updated `services/autopost/core.py`:
  - `_direction_edge_block_reason()` now filters closed trades by `autopost_perf_epoch_ts` when the epoch is set.
  - Direction-edge now enters warmup after performance reset instead of reusing stale losses.
- Runtime DB settings:
  - `autopost_perf_epoch_ts` reset at `2026-05-14 21:19:18` local time.
  - `profit_guard_min_gate_pct=70`.
  - User `quality_gate_pct=70` for `1126438536`.
- Persistent `.env` settings:
  - `DIRECTION_EDGE_GUARD_MIN_WR=0.15`.
  - `DIRECTION_EDGE_GUARD_MIN_SUM_R=-3.0`.
  - `PROFIT_GUARD_MIN_GATE_PCT=70`.

## Verification

- `python -m py_compile services\autopost\core.py` passed.
- Bot process was restarted.
- Fresh logs show:
  - `perf_epoch warmup: closed 4/20 since reset`.
  - `direction_edge warmup scalping/LONG: closed 0/4 since reset`.
  - Gate threshold changed from `need>=75%` / `need>=72%` to `need>=70%`.

## Current State

Crypto is no longer stuck behind stale direction-edge stats. No crypto trade opened immediately after tuning because current candidates were still blocked by hard filters, mostly low `Vol ratio`, stretched `VWAP`, overbought `RSI`, late `BB%B`, and weak `Stoch`.

## Next Safe Step

Keep hard blockers unchanged and observe the next market window. If crypto still never opens after enough fresh cycles, backtest or paper-test a small relaxation of volume/VWAP thresholds before applying it live.
